A very basic usage stats generator for Pokemon tournaments. Place all pokepasts inside the .txt file, then run the "Stats Generator" python script. A new csv file should then be created automatically containing the relevant usage stats for each pokemon. Make sure all the files are in the same folder before using.

The ability to calcualate the usage stats for different items is included as a seperate mode that most be enabled manually in your code editor of choice.
